---
name: xiaohongshu-publishing
description: 按产品型号整理资料，从 \\172.16.11.3 只读复制并筛选中文图片或视频，执行源文件不可变、复制完整性校验和逐项视觉验收；按九类主文案类型及内容目的、叙事方式、证据等级、分发角色四层标签编写小红书文案，执行 brief—初稿—质检—审批—发布—复盘流程；支持登录后的自动/手动/混合配图与文案模式、保存本机发布包和经确认的定时发布。用于产品图文发布、内网素材查找或小红书内容制作任务。
---

# 小红书产品图文发布

## 目标

交付一个可追溯的本地发布包；按用户选择自动、手动或混合处理产品资料、文案和素材，并在用户确认后立即发布，或保存为可追踪的定时发布任务。

## 首次使用教学模式 MUST

首次加载本 skill，或用户说“第一次使用 / 教我怎么用 / 带我走一遍”时，先进入 `首次教学`，不要直接跳到素材搜索、写文案或发布。教学目标是让用户理解并完成一遍完整流程，可选择“模拟演练”或“真实任务”。

教学顺序固定为：

1. **说明目标**：用一句话说明本次要完成的是创建、写文案、补图片、质检、保存、定时或发布中的哪些步骤。
2. **展示配置卡**：逐项解释产品、动作、文案类型、风格、配图、产品信息、可见性、定时和文件夹；先给一份可复制模板，再让用户填写。
3. **解释状态**：每进入一个状态都告诉用户现在完成了什么、为什么停在这里、下一步只需做什么。
4. **走一次主流程**：按登录 → 配置 → 创建任务包 → 产品 brief → 文案 → 图片 → 质检 → 保存/定时预览的顺序执行；模拟演练不访问共享盘、不登录、不上传、不发布。
5. **设置停顿点**：在用户必须操作的位置停下，例如登录、把手动图片放入「图片」目录、补产品资料、确认定时预览；不要替用户假设已经完成。
6. **收尾复盘**：展示任务包路径、状态、下一步动作和常见错误；询问用户是否要进入真实发布或下次直接使用快捷模式。

首次教学完成后，在本机非敏感任务记录中写入 `onboarding.completed=true`。后续默认进入快捷流程，但用户再次要求教学时必须重新展示完整流程。教学中不得保存密码、Cookie、二维码 URL 或验证码。

首次教学开场固定使用以下短模板（字段可按用户已提供内容预填）：

```text
这是第一次使用小红书发布流程，我会带你走完一遍。
你将依次学会：配置任务 → 整理资料 → 写文案 → 放入图片 → 质检 → 保存/定时/发布。
本次请选择：模拟演练（不登录、不访问共享盘、不发布）或真实任务。
```

## 首次动作

每次调用先自动准备并检查 Spider_XHS 运行环境，再检查 Creator 登录态。运行环境不存在时，自动在本机非 C 盘拉取项目、创建虚拟环境并安装依赖；缺少 Python 或 Node.js 时自动尝试使用 `winget` 安装。只有网络不可用、没有可用安装源、没有非 C 盘或系统权限不足时才报告 `受阻`，不要把安装任务转嫁给用户。

登录检查通过前，不收集产品资料、不访问共享盘、不复制素材、不写文案。

- 登录、运行环境自举和发布细则：阅读 [references/spider-xhs.md](references/spider-xhs.md)。
- 共享盘访问、复制和逐项素材检查：阅读 [references/materials.md](references/materials.md)。
- 产品简报、文案类型验收和发布前质检：阅读 [references/copywriting.md](references/copywriting.md)。
- 文案类型、路由和分类型写作规格：阅读 [references/copy-types.md](references/copy-types.md)。
- 编辑分类、证据等级、版本状态和大厂质量门：同样以 [references/copy-types.md](references/copy-types.md) 为准。
- 登录成功后发给用户的任务引导和填写模板：阅读 [references/task-config.md](references/task-config.md)。
- 外部文案引擎路由、输入输出契约和标准化质量门：阅读 [references/copywriting-engines.md](references/copywriting-engines.md)。
- 已拉取的外部 skill、固定提交和更新约定：阅读 [references/vendor-manifest.md](references/vendor-manifest.md)。
- Obsidian 文档导航层、同步边界和敏感信息禁区：阅读 [references/obsidian.md](references/obsidian.md)。

## Obsidian 文档导航层（可选）

Obsidian 只承载本 skill 的结构图、使用说明、状态解释和跳转链接，不替代非 `C:` 盘本机保存库中的 `任务索引.json`、任务包或发布记录。没有 Obsidian 时，skill 仍按本文件和本机任务索引运行。

同步时遵守以下边界：

- 任务保存、补充、发布和定时状态先写入本机任务清单与任务索引；只有用户明确要求时，才把任务摘要整理进 Obsidian。
- Obsidian 不保存 Cookie、二维码 URL、验证码、密码、DPAPI 会话快照、共享盘凭据或其他敏感认证材料。
- skill 的行为规则以 `SKILL.md` 为准；Obsidian 页面用于快速阅读和导航，发生不一致时先修正页面，不把页面当作发布依据。

## 按需任务配置（CONDITIONAL）

登录过程中不发卡。登录验收成功后，必须按 [references/task-config.md](references/task-config.md)「发给用户」发出中文选项和填写模板，请用户自己选、自己填。选项说明用普通文字；【小红书任务配置】单独用语言标记 text 的围栏代码块发出，让界面显示成「纯文本」高亮卡片，方便复制。不要把怎么选和模板塞进同一个代码块，不要用表格、JSON 或引用块代替。已经说清的字段预填进该代码块；不得猜空着的类型、风格、目标任务或定时时间。不要把字段 ID、英文名或内部对照表发给用户。

```text
【小红书任务配置】
产品：
动作：
文案类型：
文案风格：
配图：
可见性：公开
定时：无
文件夹：
```

先引导用户选定动作和产品。有定时意向时先按发布时间搜文件夹；用户写了文件夹就去指定位置，没写就去 `F:\注册机\小红书发布\`。这张卡确认的是这篇怎么写、怎么发，不是产品简报，也不是成稿。

按意图收集：

- **登录过程中**：只做登录，不发引导。
- **登录成功后**：除发布已有任务外，立刻发中文选项，并把填写模板用「纯文本」代码块高亮发出，状态 `待任务配置`。用户只说登录、还没说下一步时，同样发引导，请用户选动作。
- **发布已有任务**：不发这张卡；用任务名、产品名、任务 ID 或关键词定位，沿用已保存文案、类型、风格和素材。
- **定时发布 / 有定时意向**：先按发布时间搜文件夹，不要先新建、不要先搜共享盘。用户指定了文件夹就在指定位置搜；没指定就在默认 `F:\注册机\小红书发布\` 搜。文件夹名对应该发布时间，例如 `2026-09-20_20-00_HU01`。找到唯一包就沿用；找到多个就列出来让用户选；找不到再新建。
- **写文案 / 保存 / 创建内容**：用用户填回的模板继续；产品、文案类型、文案风格还空着就留在引导里等用户选，不替用户定。
- **先空着 / 后补内容**：产品可写“待补”，配图选“先空着”；不访问共享盘。定时或保存时仍先建 `时间_型号` 文件夹和空的「图片」目录，方便用户自己塞图。
- **补充任务**：先定位已有保存包，只问本次要补的字段。
- 未指定类型 → 种草分享；风格跟类型走。可见性默认公开。用户只回复“按默认”且是创建内容时，使用自动配图、自动文案、种草分享、口语种草、公开、生成预览等待确认；登录或已有任务发布不套用。
- 开始时确认类型和风格，不等于中途再确认成稿。最终发布预览仍要确认；用户说“直接发布”只跳过这次重复确认，不替代定时确认。

模式解释和分支约束：

- **自动配图**：按共享盘中文店铺素材优先级查找、复制到该包「图片」目录后逐项验收；**手动配图**：不主动搜索共享盘。用户可把图直接放进该任务包的「图片」文件夹，或给出本机路径/附件后再验收。**混合**：两类素材都可使用，但在清单中分别标记来源。
- **自动文案**：先按 [references/copy-types.md](references/copy-types.md) 选定文案类型，再按该类型规格生成；未指定时默认种草分享。选定类型后按默认风格写语气；用户另指定风格时，类型管结构、风格管语气。**手动文案**：保留用户提供的标题、正文和话题，不擅自改写，只做事实、敏感词、长度、格式和类型一致性质检。**混合**：把用户要点视为事实约束，由 skill 按选定类型补齐结构，不能新增未提供或未验证的功效。
- **自动获取产品信息**：登录后才访问共享盘或其他已配置资料源；**用户提供**：只使用用户资料；**混合**：用户资料优先，自动资料只用于核对和补充可追溯事实。
- 用户只回复“按默认”时，使用“自动配图、自动文案、种草分享、口语种草、自动获取产品信息、生成预览等待确认、公开”，不自动保存或定时。
- 用户选择“先创建保存任务（后补内容）”时，产品可写“待补”，配图选“先空着”；储存分类和保存名称按产品推断。文案、图片和产品资料可以留空，不访问共享盘、不生成文案、不上传素材。
- 对创建内容、补充任务和发布任务，只要当前操作所需字段缺失就标记 `待补资料`；不得用文件名、历史任务或猜测补齐。占位保存允许把未知字段明确标记为“待补”。
- 用户后续只说“发布”或“定时发布”而未给出路径时，登录验收成功后必须先检索本地任务索引；只有找到唯一匹配的未发布任务才可继续。找到多个候选时列出分类、产品、任务名和路径尾部让用户选择；找不到时报告缺少任务，不得改为搜索共享盘或猜测目标。

## 必须遵守

1. `\\172.16.11.3` 及其子目录是严格只读源：只允许浏览、读取、预览和复制已有文件到本机。禁止新增、删除、移动、剪切、重命名、编辑、裁切、压缩、转码、加水印、覆盖、替换、上传回传、改属性、改权限、生成临时文件及其他任何改变远端状态的操作。
2. 复制前保留源路径；复制目标必须是本机非 C 盘。所有改名、整理、筛选标记、裁切、压缩、转码、合成和格式转换只能对本机副本执行，结果不得写回共享盘。
3. 不在聊天、日志、HTML、文案或素材目录中索要、打印或保存明文密码、短信验证码、Cookie 或内网凭据；仅在用户明确要求持久化登录态时，允许把最小会话快照写入当前 Windows 用户/机器绑定的 DPAPI 加密文件；不绕过验证码、二次验证、风控或平台限制。
4. 创建内容前确认产品、文案类型和文案风格，属于改稿方向确认，不算产品简报确认，也不算成稿确认。只在最终发布/保存/定时预览阶段展示标题、正文、话题、素材顺序、可见性、计划时间（如有）和账号信息摘要并请求必要确认；产品简报和成稿中间不再单独打断。若用户在任务开始或过程中明确表示“直接发布/无需确认”，视为已授权，完成质检后直接发布，不重复询问；这不等于授权创建定时任务。
5. 事实、参数、价格、功效和库存必须可追溯。缺少依据就标记待确认，不得猜测或夸大。

6. **对外文案不得泄露内部溯源措辞**：`素材标注`、`图中标注`、`共享盘`、源文件路径、内部审计说明和“来自某张图”等只允许出现在产品简报、素材清单或发布记录中，禁止进入标题、正文、话题、封面文案及其他对外发布字段。对外文案应直接、自然地表达已核实事实，例如“整机约95g”，不得写成“图中标注约95g”。

7. **素材完整性 MUST**：共享盘原图是不可变源；任何“拉取”只能通过复制产生本机工作副本。不得把预览缩放、图片查看器缓存或发布端自动处理误认为已修改源文件。复制、筛选和发布全过程必须能追溯到源路径、原始文件名、本机副本和校验结果。

## 图文素材与卖点确认规则

- 用户需求为“小红书图文”时，先在共享盘进入 `平面组`，再按产品归属检查 `Aecooly`、`JSAUX` 等品牌组，进入对应型号目录。
- 图片选择优先级为：天猫 / 淘宝 / 京东等中文店铺素材（含中文参数、卖点或场景图） > 其他中文电商素材 > 英文或其他语言素材。优先使用能直接被中文用户读懂、且适合小红书竖版展示的图片。
- 若同一型号同时存在多个店铺版本，优先选信息完整、清晰、无水印冲突、能支撑文案句子的中文图；非中文图只有在没有合适中文图或用户明确同意时才使用，并在素材说明中标记原因。
- 取得图片后，先检查用户是否已提供产品详情、卖点、参数、限制条件和禁用词：有则以用户资料为主并核对图片；没有则只能依据图片中明确可见的文字和画面整理产品简报，不得补猜参数或功效。
- 产品简报仅作为内部事实底稿：用户已提供产品参数、卖点和限制条件时，直接进入成稿，不停留、不询问确认；资料缺口在内部标记“待确认”，不得靠猜测补齐。只有缺少关键事实会导致文案无法真实落地时，才暂停并说明缺口。

## 工作流与交付物

1. **运行时与登录门**：先执行 `scripts/launch_creator_login_panel.ps1`（已有环境时快速复用并验证）；再检查已有会话。确认未登录后，优先启动该脚本提供本机 `127.0.0.1` 登录面板，面板可选择二维码或手机号登录；登录成功后仅将最小会话快照写入非 `C:` 盘的 Windows DPAPI 加密文件，Cookie、二维码 URL、验证码和最终 Auth 不进入 HTML，HTML 只接收脱敏状态。服务启动时自动解密并重新执行 Creator `bootstrap()`，验收失败则清除失效缓存并回到登录页。若无法启动面板，二维码流程才使用 `scripts/creator_login_qr.py` 生成本机非 C 盘二维码图片并展示给用户扫码。成功后再次验收 Creator 用户信息。不能只报告“未登录”而不提供可执行登录入口。
2. **意图与按需配置门**：登录过程中不发引导。登录验收成功后，除发布已有任务外，立刻发出中文任务选项和填写模板，请用户自己选。用户已经说清的字段预填；空着的等用户填回，不替用户定类型和风格。储存分类和保存名称按产品推断，不先问。用户后续只说“发布”或“定时发布”且未给路径时，先按“任务库检索与重复发送防护”定位本地保存任务，再继续后续门。
3. **目录与素材发现门**：仅在选择自动或混合配图时查看共享盘 `平面组`，再查看 `Aecooly`、`JSAUX` 等品牌组及其型号目录；按中文店铺素材优先级筛选候选图片，保留源路径，先不上传。纯手动配图只处理用户提供的图片。
4. **产品资料门**：按配置决定是否自动获取产品详情/卖点/参数；用户资料优先，缺口留在内部清单，不得猜测补齐。
5. **小红书文案门**：先确定文案类型和风格（与储存分类分开），再按配置选择自动、手动或混合文案。自动文案按该类型规格和文案引擎生成，语气跟风格走；手动文案只质检不擅自改写；混合文案只能在用户要点和已核实事实范围内按该类型补齐结构。类型写入 `copy.copy_type`，风格写入 `copy.copy_style`；未指定风格时跟类型走。
6. **素材复制与检查门**：将自动筛选素材或用户手动素材复制到本机结构化目录，保留源路径/原始文件名，核对数量、大小、哈希（可行时）和可打开性；逐张图片、逐条视频查看并标记 `保留 / 备选 / 淘汰`，只把保留项复制到待发布目录，精选预览不得直接视为待发布成品。
7. **保存门**：
   - 用户选择“仅保存”或“保存并定时发布”时，先按 `时间_型号` 创建本机保存包（含空的「图片」文件夹），再按已有文案和图片做质检；保存动作不调用发布接口。选择“保存并定时发布”且用户已确认文案和计划时间时，即使手动图片尚未补齐，也可以注册定时任务，状态记为 `待素材检查` 或 `定时发布中`；到点前必须复查图片，缺图则提醒并暂停发布。
   - 用户选择“先创建保存任务（后补内容）”或配图“先空着”时，先创建 `时间_型号` 目录、空的「图片」文件夹和任务清单，状态记为 `已创建待补内容`。把「图片」完整路径告诉用户。该动作不访问共享盘、不上传、不发布、不注册定时。
8. **发布门**：立即发布或普通发布流程最后一次性展示标题、正文、话题、账号摘要、可见性（默认公开）和素材顺序/预览；等用户明确确认后，再次 `bootstrap()` 验收登录态并调用发布接口。若用户已明确要求直接发布，则跳过这次重复确认，直接执行。发布请求默认使用 `privacy_info.type=0`（公开），只有用户明确要求时才使用其他可见性。
9. **定时发布门**：定时任务必须先保存成功、展示定时预览并取得用户明确确认；到达计划时间时，先复查用户是否已补充手动图片、产品资料缺口、文件可打开性、哈希和视觉验收。任一项未完成，立即提醒用户并保持 `待补资料` 或 `待素材检查`，不得上传、发布或丢失计划；用户补齐后必须再次完成验收并明确确认发布，才重新 `bootstrap()` 验收登录态并调用发布接口。若定时执行器不可用、登录失效或平台拒绝定时参数，保持保存包不变并报告 `受阻` 或 `待重新登录`，不得伪造“已定时”。
10. **记录门**：成功反馈和笔记链接可取得时才标记 `已发布`；同时记录保存时间、计划发布时间、实际发布时间、链接、文案版本、采用的文案引擎与规则版本、素材清单和异常，并在任务清单、本地任务索引和发布记录中写入 `已发布` 标签，作为后续重复发送拦截依据。

## 保存包与定时任务

保存只写入用户指定的非 `C:` 盘本机目录，不写回共享盘。默认根目录 `F:\注册机\小红书发布\`。包名按 `时间_型号`：定时用计划时间 `YYYY-MM-DD_HH-mm_型号`，保存或写文案落盘用当天 `YYYY-MM-DD_型号`。名称含非法字符时改成安全短名，清单里保留原名。

```text
<非C盘根目录>\小红书发布\
├─任务索引.json
└─<时间>_<型号>\
   ├─产品资料\
   ├─图片\
   ├─视频\
   ├─文案笔记.md
   └─任务清单.json
```

有定时意向时先搜再新建：用户指定了文件夹就在指定位置搜，没指定就在 `F:\注册机\小红书发布\` 搜。文件夹名对应该发布时间。找不到再新建，并建好空的「图片」文件夹，把完整路径告诉用户。型号从产品字段取（如 HU01）；取不到时用安全短名。重名时加 `_2`，不覆盖已有包。用户确认文案和计划时间后，即使「图片」为空也可注册定时；但图片未通过到点复查时不得上传或发布，必须提醒用户补图并等待明确确认。

`任务清单.json` 至少记录：任务原始名称、产品名称/型号、创建时间、任务配置（配图/文案/产品信息模式）、文案类型 ID 与名称、文案风格 ID 与名称、标题/正文/话题版本、素材源路径与目标路径、文件大小和哈希状态、可见性、计划发布时间及时区、当前状态、实际发布时间/链接（如有）和异常。`assets.drop_folder` 记为 `图片`。不得写入 Cookie、二维码 URL、短信验证码、明文密码或其他凭据。

每个保存库根目录同时维护一个本地 `任务索引.json`（例如 `<非C盘根目录>\小红书发布\任务索引.json`），每条记录至少包含 `task_id`、储存分类、产品名称/型号、任务名称、保存包完整路径、当前状态、标签、创建/更新时间和最后一次发布结果。保存包路径发生变化时必须同步更新索引；索引只写本机非 `C:` 盘，不写回共享盘。

## 任务库检索与重复发送防护

### 定时先搜文件夹

用户有定时意向时，先搜本机文件夹，再决定沿用还是新建。不要先搜共享盘，也不要先新建。

1. 定搜索位置。用户指定了文件夹就用指定路径，必须是本机非 C 盘。没指定就用默认 `F:\注册机\小红书发布\`。
2. 指定路径如果本身已是任务包（有 `任务清单.json`、`文案笔记.md` 或「图片」），直接用这个包，不再往下搜。
3. 指定路径如果是上级目录，只看这一层子文件夹，不整盘递归。
4. 用发布时间对文件夹名。`2026-09-20 20:00` 对应 `2026-09-20_20-00`。优先全名 `时间_型号`，其次名称以该时间开头的文件夹。
5. 同时读该根目录里的 `任务索引.json`，按同一时间匹配。
6. 只命中一个：打开该包，检查文案和「图片」。
7. 命中多个：列出文件夹名和路径尾部，让用户选。
8. 一个都没有：再在同一根目录新建 `时间_型号`，并建空的「图片」文件夹。

### 默认检索顺序

用户下一次只说“发布”或“定时发布”时，登录验收成功后按以下顺序处理：

1. 先读取已记录的 `任务索引.json`，按任务 ID、时间_型号包名、产品名称、笔记标题依次匹配；不先搜索共享盘。
2. 只有一个匹配且任务状态为 `已保存` 或 `待确认发布` 时，读取该保存包的 `任务清单.json` 并核对路径、文案和素材是否仍可打开，再进入发布或定时预览。
3. 匹配多个时，只展示分类、产品、任务名称、状态和路径尾部，要求用户选择；匹配为零、路径失效或清单缺失时，报告缺口并保持原任务不变，不得猜测目标。
4. `已创建待补内容`、`待补资料`、`待素材检查` 的任务不能直接发布或定时；先引导用户补齐缺失内容。若索引不存在，只能在用户已记录的保存库根目录内查找 `任务清单.json` 并重建索引，不能扫描整盘。

### 发布标签与拦截规则

- 发布成功后，在 `任务清单.json` 的 `tags` 写入 `已发布`，在 `06_发布记录` 写入 `标签_已发布.json`，并把同一标签、笔记 ID、链接和实际发布时间同步到 `任务索引.json`。这是储存库内的可见标签，不是对外发布话题。
- 任务带有 `已发布` 标签或状态为 `已发布` 时，默认阻止再次调用 `post_note()`；向用户说明已发布的时间和笔记 ID/链接，并要求明确说“重新发布”后才允许创建新的任务 ID 或修订版本。
- 任务状态为 `待定时发布` 或 `定时发布中` 时，默认阻止重复注册计划；如需改时间，先更新原计划，不创建第二个调度。取消、失败或重新计划都要保留历史记录。
- “已上传”“已保存”“已定时”都不能替代 `已发布` 标签；只有取得明确成功信息或笔记链接才添加该标签。

### 先保存、后补内容

“先创建保存任务（后补内容）”是一个独立的占位流程，不等同于“已保存可发布稿”：

1. 创建包前，定时先按发布时间搜文件夹。找不到再新建 `时间_型号`，并建好空的「图片」文件夹。`任务清单.json` 将 `copy.status`、`assets.status` 和 `product_info.status` 标记为 `待补`，`assets.drop_folder` 记为 `图片`。
2. 后续用户说“补充某个保存任务”时，先按保存名称或任务 ID 定位原包，再只更新用户提供的文案、图片或产品资料；手动图片仍须复制到本机非 `C:` 盘并完成哈希与视觉验收，自动素材仍须遵守共享盘只读规则。
3. 文案、素材和产品资料全部补齐并通过质检后，状态才可从 `已创建待补内容` 转为 `已保存`，或进入 `待确认发布` / `待定时发布`；补齐前不得调用 `post_note()`，不得登记计划时间，不得把空包报告为“可发布”。
4. 占位保存包允许长期保留；取消或删除只针对本机保存包，不影响共享盘源文件。

定时发布遵循以下边界：

1. 计划时间必须是未来时间，统一按 `Asia/Shanghai` 解释并在预览中显示完整日期和时区；未填写计划时间时不得创建定时任务。
2. 保存成功不等于已定时，已定时也不等于已发布；只有调度注册成功才记录 `待定时发布`。到点先做补充资料/图片复查，取得用户补齐后的明确发布确认并成功发布，才改为 `已发布`。
3. 当前 Spider_XHS 适配器已验证使用 `post_note` 的可选 `postTime`（13 位 Unix 毫秒时间戳）表达定时；仅在用户确认后传入。适配器版本变更或字段未验证时，才退回已配置的本机定时执行器；两者都不可用时只完成 `已保存`，并说明缺口。
4. 调度器只保存任务包路径和非敏感元数据；执行时在同一台机器重新验收 DPAPI 会话。会话失效、验证码、二次验证、风控或平台限制都必须停止并通知用户，不得绕过。
5. 修改、取消或重排计划只更新任务清单和调度记录，不删除素材副本；回滚点是整个本机保存包。

## 素材完整性与审计规范（v1.1）

### 不可变源与复制语义

- `\\172.16.11.3` 只允许读；“拉取、取图、下载素材”在本 skill 中统一解释为“复制到本机非 C 盘”，不得使用移动、剪切、重命名源文件或任何会写回源端的操作。
- 复制前必须记录源完整路径和原始文件名；本机副本默认保留原始文件名，发生同名冲突时使用新目录或明确的本机后缀，不覆盖源文件。
- 图片查看、缩略图生成、浏览器展示和 `view_image` 等操作只读；显示端自动缩放不改变本机文件。任何裁切、压缩、加字、调色、转码或合成，只能对本机副本执行。

### 分层目录与单向流转

本机发布包至少遵循以下单向流转，禁止逆向写回共享盘：

`图片` / `视频`（用户塞入或复制来的工作副本）→ 清单标记 `保留 / 备选 / 淘汰` → 只把保留项用于发布。

用户未确认前不得进入上传步骤。淘汰项留在原文件夹并在清单标明，不写回共享盘。

### 完整性校验 MUST

复制完成后必须记录：源路径、目标路径、文件名、扩展名、字节大小、图片尺寸或视频时长、可打开性和复制状态。对源端可正常读取的文件，优先执行 SHA-256 校验并记录源哈希与副本哈希；两者不一致时立即停止该文件的后续处理并重新复制。若网络源端无法稳定计算哈希，必须明确记录“源哈希未完成”，同时完成字节大小、扩展名、可打开性和本机副本哈希检查，不得把未完成校验写成“完整性通过”。

交付前再做一次本机副本验收：精选预览与候选原图的哈希应一致；发布成品如经过裁切或压缩，必须标记为“派生文件”，并保留其来源文件和处理说明。

### 逐图视觉验收与审计清单 MUST

每张图片、每条视频都要实际查看并在本机清单中写明：方向/比例、主体清晰度、场景真实性、文字可读性、水印或平台标识、是否存在敏感/冲突信息、与文案的对应句子，以及 `保留 / 备选 / 淘汰` 结论和一句理由。不得只凭文件名、缩略图或拍摄组名称决定去留。

清单还必须记录筛选日期、执行人（本任务代理）、采用的视觉参考来源 URL（如有）和用户最终选择；用户未选择前状态保持 `待素材检查`，不得自动发布。

### 回滚与破坏性边界

- 发现误操作风险、哈希不一致、源路径不明或工具行为可能写回共享盘时，立即停止并报告，不尝试“继续试试看”。
- 本地整理出错时，只删除或重建本机发布包中的副本；不得通过删除源文件来“清理重复”。
- 任务回滚点是本机发布包目录和其清单，不能把共享盘原图纳入回滚目标。发布前的预览、复制和检查均不改变远端状态。


## 文案类型规格（v1.0）

文案类型决定这一篇怎么写，储存分类只决定任务怎么归档。两者不要混用。完整规格见 [references/copy-types.md](references/copy-types.md)。

| 用户怎么说 | 文案类型 | 写法 |
|---|---|---|
| 种草、好物分享、日常笔记；或创建内容未指定 | `seeding` 种草分享 | 生活痛点开头，口语种草，参数放小抄 |
| 品宣、官方稿、按卖点策划/方案写、不要默认种草 | `official` 官方品宣 | 按方案卖点顺序，克制品宣，不走人设 |
| 首发、上市、新品 | `launch` 新品发布 | 先讲差异和怎么用，默认克制品宣 |
| 种草加点品宣、不要太正式、综合稿 | `hybrid` 种草品宣综合 | 轻松综合：口语讲方案核心卖点 |
| 测评、对比、值不值得 | `review` 测评对比 | 问答克制，只写已核实差异 |
| 怎么用、开箱、步骤 | `tutorial` 教程指南 | 步骤说明，缺步骤就待补 |
| 几点、合集、场景清单 | `listicle` 清单盘点 | 步骤说明，3–5 个可追溯卖点 |
| 避坑、FAQ、常见问题 | `faq` 问答避坑 | 问答克制，只答资料里的边界 |
| 活动、优惠、大促 | `campaign` 活动促销 | 克制品宣，规则和价格必须可追溯 |

点名优先。发布已有任务时沿用保存的 `copy.copy_type` 和 `copy.copy_style`。风格跟类型走；用户另指定时类型管结构、风格管语气。需要两种结构时另开版本，除非主类型就是综合稿。

### 文案验收门

成稿必须满足所选类型规格和公共 MUST：事实可追溯；无虚构亲测/回购/效果；无内部路径；标题、段落、话题数量符合该类型；每个卖点能追溯到简报或可见资料。种草和综合稿要有具体场景开头；品宣和新品稿不得套种草人设。未通过时不调用发布接口。

## 调用语义

用户提出真实业务需求时，使用用户原始业务目标作为任务提示，不把它改写成“测试”、冒烟或演示。登录成功后先发中文任务引导和填写模板，请用户自己选动作、产品、类型和风格；用户说「按默认」才用种草分享、口语种草。已有任务发布或定时发布先检索本地 `任务索引.json`，默认沿用已保存文案、素材、产品资料、文案类型和风格，不发填写卡。用户说“按方案写品宣”“种草加点品宣”时，预填 `official` 或 `hybrid` 进模板让用户确认，不要悄悄改掉。若用户明确说直接发布，则完成质检后直接发布；这不自动替代定时任务确认。登录前不访问共享盘、不复制素材、不写文案。

## 状态

每次汇报使用一个状态：`待登录`、`待任务配置`、`待补资料`、`待复制素材`、`待素材检查`、`待保存`、`已创建待补内容`、`已保存`、`待确认发布`、`待定时发布`、`定时发布中`、`待重新登录`、`已发布` 或 `受阻`。说明状态变化、已完成的交付物和下一步最小动作；登录成功后进入 `待任务配置`，并写出已发给用户的选项；只有发布已有任务时才跳过这张卡。“已上传”不等于“已发布”，“已创建待补内容”不等于“已保存”，“已保存”不等于“已定时”。用户已明确直接发布时，不进入`待确认发布`停留。
