---
name: xiaohongshu-publishing
description: 按产品型号整理资料，从 \\172.16.11.3 只读复制并筛选中文图片或视频，执行源文件不可变、复制完整性校验和逐项视觉验收，编写自然的小红书文案，支持登录后的自动/手动/混合配图与文案模式、保存本机发布包和经确认的定时发布；支持 zhongcao 单篇种草与 xiaohongshu-skills 品牌内容路由，使用 Spider_XHS 检查或二维码登录 Creator 并在用户确认后发布。用于产品图文发布、内网素材查找或小红书内容制作任务。
---

# 小红书产品图文发布

## 目标

交付一个可追溯的本地发布包；按用户选择自动、手动或混合处理产品资料、文案和素材，并在用户确认后立即发布，或保存为可追踪的定时发布任务。

## 首次动作

每次调用先自动准备并检查 Spider_XHS 运行环境，再检查 Creator 登录态。运行环境不存在时，自动在本机非 C 盘拉取项目、创建虚拟环境并安装依赖；缺少 Python 或 Node.js 时自动尝试使用 `winget` 安装。只有网络不可用、没有可用安装源、没有非 C 盘或系统权限不足时才报告 `受阻`，不要把安装任务转嫁给用户。

登录检查通过前，不收集产品资料、不访问共享盘、不复制素材、不写文案。

- 登录、运行环境自举和发布细则：阅读 [references/spider-xhs.md](references/spider-xhs.md)。
- 共享盘访问、复制和逐项素材检查：阅读 [references/materials.md](references/materials.md)。
- 产品简报、自然文案和发布前质检：阅读 [references/copywriting.md](references/copywriting.md)。
- 外部文案引擎路由、输入输出契约和标准化质量门：阅读 [references/copywriting-engines.md](references/copywriting-engines.md)。
- 已拉取的外部 skill、固定提交和更新约定：阅读 [references/vendor-manifest.md](references/vendor-manifest.md)。

## 按需任务配置（CONDITIONAL）

Creator 登录验收成功后，先识别用户意图，再收集当前操作所需的最少信息。下面的模板是可选配置，不是每次任务必须发送的表单；不得因为模板未填写就擅自推断模式、目标任务或定时时间。

```text
【可选任务配置】
操作：登录 / 检查登录 / 创建内容 / 补充任务 / 发布 / 定时发布 / 仅保存
目标任务或产品：
储存分类：新品宣发 / 日常种草 / 活动促销 / 其他
配图：自动获取 / 手动提供 / 混合 / 沿用已保存素材
文案：自动生成 / 手动提供 / 混合 / 沿用已保存文案
产品信息：自动获取 / 用户提供 / 混合 / 沿用已保存资料
手动图片路径或附件：
手动文案或要点：
产品名称与型号：
定时发布时间：<YYYY-MM-DD HH:mm；仅定时发布填写，时区 Asia/Shanghai>
可见性：公开（默认）/ 仅自己可见
保存名称：新品宣发—<产品名称>—<任务短名>
备注：
```

按意图收集最少字段：

- **登录 / 检查登录**：只执行运行时与登录检查，不要求填写其他字段，不访问共享盘、不写文案。
- **发布已有任务**：用户提供任务名称、产品名称、任务 ID 或可识别关键词之一即可；若未提供目标，按“任务库检索与重复发送防护”定位本地任务。默认沿用任务中已保存的文案、素材和产品资料。
- **定时发布已有任务**：除目标任务外，只要求明确的未来时间；默认沿用已保存内容，不要求重新填写配图和文案模式。
- **创建内容 / 仅保存 / 先创建保存任务**：只询问当前缺少的配图、文案、产品资料和储存信息；用户不需要的字段保持空白。
- **补充任务**：先定位已有保存包，只询问本次要补充的字段；用户只补图片时不重新询问文案，反之亦然。
- 用户只回复“按默认”时，仅在创建内容场景使用“自动配图、自动文案、自动获取产品信息、生成预览等待确认、公开”；登录或已有任务发布场景不套用该默认内容。

模式解释和分支约束：

- **自动配图**：按共享盘中文店铺素材优先级查找、复制和逐项验收；**手动配图**：不主动搜索共享盘，只接收用户明确给出的本机路径或附件，并将其复制到非 `C:` 盘发布包后按同一完整性和视觉清单验收；**混合**：两类素材都可使用，但在清单中分别标记来源。
- **自动文案**：按本 skill 的文案门生成；**手动文案**：保留用户提供的标题、正文和话题，不擅自改写，只做事实、敏感词、长度和格式质检；**混合**：把用户要点视为事实约束，由 skill 补齐结构，不能新增未提供或未验证的功效。
- **自动获取产品信息**：登录后才访问共享盘或其他已配置资料源；**用户提供**：只使用用户资料；**混合**：用户资料优先，自动资料只用于核对和补充可追溯事实。
- 用户只回复“按默认”时，使用“自动配图、自动文案、自动获取产品信息、生成预览等待确认、公开”，不自动保存或定时。
- 用户选择“先创建保存任务（后补内容）”时，只收集储存分类、产品名称/型号和保存名称等最小元数据；文案、图片和产品资料可以留空，不访问共享盘、不生成文案、不上传素材。
- 对创建内容、补充任务和发布任务，只要当前操作所需字段缺失就标记 `待补资料`；不得用文件名、历史任务或猜测补齐。占位保存允许把未知字段明确标记为“待补”。
- 用户后续只说“发布”或“定时发布”而未给出路径时，登录验收成功后必须先检索本地任务索引；只有找到唯一匹配的未发布任务才可继续。找到多个候选时列出分类、产品、任务名和路径尾部让用户选择；找不到时报告缺少任务，不得改为搜索共享盘或猜测目标。

## 必须遵守

1. `\\172.16.11.3` 及其子目录是严格只读源：只允许浏览、读取、预览和复制已有文件到本机。禁止新增、删除、移动、剪切、重命名、编辑、裁切、压缩、转码、加水印、覆盖、替换、上传回传、改属性、改权限、生成临时文件及其他任何改变远端状态的操作。
2. 复制前保留源路径；复制目标必须是本机非 C 盘。所有改名、整理、筛选标记、裁切、压缩、转码、合成和格式转换只能对本机副本执行，结果不得写回共享盘。
3. 不在聊天、日志、HTML、文案或素材目录中索要、打印或保存明文密码、短信验证码、Cookie 或内网凭据；仅在用户明确要求持久化登录态时，允许把最小会话快照写入当前 Windows 用户/机器绑定的 DPAPI 加密文件；不绕过验证码、二次验证、风控或平台限制。
4. 只在最终发布/保存/定时预览阶段展示标题、正文、话题、素材顺序、可见性、计划时间（如有）和账号信息摘要并请求必要确认；产品简报和文案中间不再单独打断用户确认。若用户在任务开始或过程中明确表示“直接发布/无需确认”，视为已授权，完成质检后直接发布，不重复询问；这不等于授权创建定时任务。
5. 事实、参数、价格、功效和库存必须可追溯。缺少依据就标记待确认，不得猜测或夸大。

6. **对外文案不得泄露内部溯源措辞**：`素材标注`、`图中标注`、`共享盘`、源文件路径、内部审计说明和“来自某张图”等只允许出现在产品简报、素材清单或发布记录中，禁止进入标题、正文、话题、封面文案及其他对外发布字段。对外文案应直接、自然地表达已核实事实，例如“整机约95g”，不得写成“图中标注约95g”。

7. **素材完整性 MUST**：共享盘原图是不可变源；任何“拉取”只能通过复制产生本机工作副本。不得把预览缩放、图片查看器缓存或发布端自动处理误认为已修改源文件。复制、筛选和发布全过程必须能追溯到源路径、原始文件名、本机副本和校验结果。

## 图文素材与卖点确认规则

- 用户需求为“小红书图文”时，先在共享盘进入 `平面组`，再按产品归属检查 `Aecooly`、`JSAUX` 等品牌组，进入对应型号目录。
- 图片选择优先级为：天猫 / 淘宝 / 京东等中文店铺素材（含中文参数、卖点或场景图） > 其他中文电商素材 > 英文或其他语言素材。优先使用能直接被中文用户读懂、且适合小红书竖版展示的图片。
- 若同一型号同时存在多个店铺版本，优先选信息完整、清晰、无水印冲突、能支撑文案句子的中文图；非中文图只有在没有合适中文图或用户明确同意时才使用，并在素材说明中标记原因。
- 取得图片后，先检查用户是否已提供产品详情、卖点、参数、限制条件和禁用词：有则以用户资料为主并核对图片；没有则只能依据图片中明确可见的文字和画面整理产品简报，不得补猜参数或功效。
- 产品简报仅作为内部事实底稿：用户已提供产品参数、卖点和限制条件时，直接进入成稿，不停留、不询问确认；资料缺口在内部标记“待确认”，不得靠猜测补齐。只有缺少关键事实会导致文案无法真实落地时，才暂停并说明缺口。

## 工作流与交付物

1. **运行时与登录门**：先执行 `scripts/launch_creator_login_panel.ps1`（已有环境时快速复用并验证）；再检查已有会话。确认未登录后，优先启动该脚本提供本机 `127.0.0.1` 登录面板，面板可选择二维码或手机号登录；登录成功后仅将最小会话快照写入非 `C:` 盘的 Windows DPAPI 加密文件，Cookie、二维码 URL、验证码和最终 Auth 不进入 HTML，HTML 只接收脱敏状态。服务启动时自动解密并重新执行 Creator `bootstrap()`，验收失败则清除失效缓存并回到登录页。若无法启动面板，二维码流程才使用 `scripts/creator_login_qr.py` 生成本机非 C 盘二维码图片并展示给用户扫码。成功后再次验收 Creator 用户信息。不能只报告“未登录”而不提供可执行登录入口。
2. **意图与按需配置门**：先判断用户是登录、检查登录、创建内容、补充任务、发布已有任务、定时发布已有任务还是仅保存；只收集该动作缺少的字段，不强制用户填写完整模板。用户后续只说“发布”或“定时发布”且未给路径时，先按“任务库检索与重复发送防护”定位本地保存任务，再继续后续门。
3. **目录与素材发现门**：仅在选择自动或混合配图时查看共享盘 `平面组`，再查看 `Aecooly`、`JSAUX` 等品牌组及其型号目录；按中文店铺素材优先级筛选候选图片，保留源路径，先不上传。纯手动配图只处理用户提供的图片。
4. **产品资料门**：按配置决定是否自动获取产品详情/卖点/参数；用户资料优先，缺口留在内部清单，不得猜测补齐。
5. **小红书文案门**：按配置选择自动、手动或混合文案；自动文案按本 skill 的文案引擎规则生成，手动文案只质检不擅自改写，混合文案只能在用户要点和已核实事实范围内补齐结构。
6. **素材复制与检查门**：将自动筛选素材或用户手动素材复制到本机结构化目录，保留源路径/原始文件名，核对数量、大小、哈希（可行时）和可打开性；逐张图片、逐条视频查看并标记 `保留 / 备选 / 淘汰`，只把保留项复制到待发布目录，精选预览不得直接视为待发布成品。
7. **保存门**：
   - 用户选择“仅保存”或“保存并定时发布”时，先完成同一套文案、素材和事实质检，再创建本机保存包并写入任务清单和本地任务索引；保存动作不调用发布接口。
   - 用户选择“先创建保存任务（后补内容）”时，可以先创建只有目录和任务清单的占位保存包；文案、图片、产品资料和哈希字段允许为空，并将状态记为 `已创建待补内容`。该动作不访问共享盘、不写入虚构内容、不上传、不发布、不创建定时任务。
8. **发布门**：立即发布或普通发布流程最后一次性展示标题、正文、话题、账号摘要、可见性（默认公开）和素材顺序/预览；等用户明确确认后，再次 `bootstrap()` 验收登录态并调用发布接口。若用户已明确要求直接发布，则跳过这次重复确认，直接执行。发布请求默认使用 `privacy_info.type=0`（公开），只有用户明确要求时才使用其他可见性。
9. **定时发布门**：定时任务必须先保存成功、展示定时预览并取得用户明确确认；到点才重新 `bootstrap()` 验收登录态并调用发布接口，不得提前上传或提前发布。若定时执行器不可用、登录失效或平台拒绝定时参数，保持保存包不变并报告 `受阻` 或 `待重新登录`，不得伪造“已定时”。
10. **记录门**：成功反馈和笔记链接可取得时才标记 `已发布`；同时记录保存时间、计划发布时间、实际发布时间、链接、文案版本、采用的文案引擎与规则版本、素材清单和异常，并在任务清单、本地任务索引和发布记录中写入 `已发布` 标签，作为后续重复发送拦截依据。

## 保存包与定时任务

保存只写入用户指定的非 `C:` 盘本机目录，不写回共享盘。默认包名为 `新品宣发—<产品名称>—<任务短名>`；若名称含路径分隔符或过长，使用本机安全短名，并在清单中保留用户原始名称。

```text
<非C盘根目录>\小红书发布\
├─任务索引.json
└─新品宣发—<产品名称>—<任务短名>\
   ├─00_产品资料\
   ├─01_文案\发布稿.md
   ├─02_原始素材_图片\
   ├─03_原始素材_视频\
   ├─04_已筛选_待发布\
   ├─05_发布成品\
   └─06_发布记录\任务清单.json
```

`任务清单.json` 至少记录：任务原始名称、产品名称/型号、创建时间、任务配置（配图/文案/产品信息模式）、标题/正文/话题版本、素材源路径与目标路径、文件大小和哈希状态、可见性、计划发布时间及时区、当前状态、实际发布时间/链接（如有）和异常。不得写入 Cookie、二维码 URL、短信验证码、明文密码或其他凭据。

每个保存库根目录同时维护一个本地 `任务索引.json`（例如 `<非C盘根目录>\小红书发布\任务索引.json`），每条记录至少包含 `task_id`、储存分类、产品名称/型号、任务名称、保存包完整路径、当前状态、标签、创建/更新时间和最后一次发布结果。保存包路径发生变化时必须同步更新索引；索引只写本机非 `C:` 盘，不写回共享盘。

## 任务库检索与重复发送防护

### 默认检索顺序

用户下一次只说“发布”或“定时发布”时，登录验收成功后按以下顺序处理：

1. 先读取已记录的 `任务索引.json`，按任务 ID、完整保存名称、储存分类 + 产品名称 + 任务名称、笔记标题依次匹配；不先搜索共享盘。
2. 只有一个匹配且任务状态为 `已保存` 或 `待确认发布` 时，读取该保存包的 `任务清单.json` 并核对路径、文案和素材是否仍可打开，再进入发布或定时预览。
3. 匹配多个时，只展示分类、产品、任务名称、状态和路径尾部，要求用户选择；匹配为零、路径失效或清单缺失时，报告缺口并保持原任务不变，不得猜测目标。
4. `已创建待补内容`、`待补资料`、`待素材检查` 的任务不能直接发布或定时；先引导用户补齐缺失内容。若索引不存在，只能在用户已记录的保存库根目录内查找 `任务清单.json` 并重建索引，不能扫描整盘。

### 发布标签与拦截规则

- 发布成功后，在 `任务清单.json` 的 `tags` 写入 `已发布`，在 `06_发布记录` 写入 `标签_已发布.json`，并把同一标签、笔记 ID、链接和实际发布时间同步到 `任务索引.json`。这是储存库内的可见标签，不是对外发布话题。
- 任务带有 `已发布` 标签或状态为 `已发布` 时，默认阻止再次调用 `post_note()`；向用户说明已发布的时间和笔记 ID/链接，并要求明确说“重新发布”后才允许创建新的任务 ID 或修订版本。
- 任务状态为 `待定时发布` 或 `定时发布中` 时，默认阻止重复注册计划；如需改时间，先更新原计划，不创建第二个调度。取消、失败或重新计划都要保留历史记录。
- “已上传”“已保存”“已定时”都不能替代 `已发布` 标签；只有取得明确成功信息或笔记链接才添加该标签。

### 先保存、后补内容

“先创建保存任务（后补内容）”是一个独立的占位流程，不等同于“已保存可发布稿”：

1. 创建包时至少填写储存分类（如 `新品宣发`）、产品名称/型号（未知时明确写“待补”）和保存名称；目录按本节结构创建，`任务清单.json` 中将 `copy.status`、`assets.status` 和 `product_info.status` 标记为 `待补`。
2. 后续用户说“补充某个保存任务”时，先按保存名称或任务 ID 定位原包，再只更新用户提供的文案、图片或产品资料；手动图片仍须复制到本机非 `C:` 盘并完成哈希与视觉验收，自动素材仍须遵守共享盘只读规则。
3. 文案、素材和产品资料全部补齐并通过质检后，状态才可从 `已创建待补内容` 转为 `已保存`，或进入 `待确认发布` / `待定时发布`；补齐前不得调用 `post_note()`，不得登记计划时间，不得把空包报告为“可发布”。
4. 占位保存包允许长期保留；取消或删除只针对本机保存包，不影响共享盘源文件。

定时发布遵循以下边界：

1. 计划时间必须是未来时间，统一按 `Asia/Shanghai` 解释并在预览中显示完整日期和时区；未填写计划时间时不得创建定时任务。
2. 保存成功不等于已定时，已定时也不等于已发布；只有调度注册成功才记录 `待定时发布`，到点执行后取得明确成功信息才改为 `已发布`。
3. 当前 Spider_XHS 适配器已验证使用 `post_note` 的可选 `postTime`（13 位 Unix 毫秒时间戳）表达定时；仅在用户确认后传入。适配器版本变更或字段未验证时，才退回已配置的本机定时执行器；两者都不可用时只完成 `已保存`，并说明缺口。
4. 调度器只保存任务包路径和非敏感元数据；执行时在同一台机器重新验收 DPAPI 会话。会话失效、验证码、二次验证、风控或平台限制都必须停止并通知用户，不得绕过。
5. 修改、取消或重排计划只更新任务清单和调度记录，不删除素材副本；回滚点是整个本机保存包。

## 素材完整性与审计规范（v1.1）

### 不可变源与复制语义

- `\\172.16.11.3` 只允许读；“拉取、取图、下载素材”在本 skill 中统一解释为“复制到本机非 C 盘”，不得使用移动、剪切、重命名源文件或任何会写回源端的操作。
- 复制前必须记录源完整路径和原始文件名；本机副本默认保留原始文件名，发生同名冲突时使用新目录或明确的本机后缀，不覆盖源文件。
- 图片查看、缩略图生成、浏览器展示和 `view_image` 等操作只读；显示端自动缩放不改变本机文件。任何裁切、压缩、加字、调色、转码或合成，只能对本机副本执行。

### 分层目录与单向流转

本机发布包至少遵循以下单向流转，禁止逆向写回共享盘：

`02_原始素材_图片/视频`（复制来的工作副本） → `04_已筛选_待发布`（通过逐项验收的副本） → `05_发布成品`（必要的本机裁切/压缩/排版结果） → Creator 上传。

每一步都保留上一层文件，不做原地覆盖；用户未确认前不得进入上传步骤。仅为预览而复制的文件应放入独立的“精选预览/待确认”子目录，不能冒充“已筛选_待发布”。

### 完整性校验 MUST

复制完成后必须记录：源路径、目标路径、文件名、扩展名、字节大小、图片尺寸或视频时长、可打开性和复制状态。对源端可正常读取的文件，优先执行 SHA-256 校验并记录源哈希与副本哈希；两者不一致时立即停止该文件的后续处理并重新复制。若网络源端无法稳定计算哈希，必须明确记录“源哈希未完成”，同时完成字节大小、扩展名、可打开性和本机副本哈希检查，不得把未完成校验写成“完整性通过”。

交付前再做一次本机副本验收：精选预览与候选原图的哈希应一致；发布成品如经过裁切或压缩，必须标记为“派生文件”，并保留其来源文件和处理说明。

### 逐图视觉验收与审计清单 MUST

每张图片、每条视频都要实际查看并在本机清单中写明：方向/比例、主体清晰度、场景真实性、文字可读性、水印或平台标识、是否存在敏感/冲突信息、与文案的对应句子，以及 `保留 / 备选 / 淘汰` 结论和一句理由。不得只凭文件名、缩略图或拍摄组名称决定去留。

清单还必须记录筛选日期、执行人（本任务代理）、采用的视觉参考来源 URL（如有）和用户最终选择；用户未选择前状态保持 `待素材检查`，不得自动发布。

### 回滚与破坏性边界

- 发现误操作风险、哈希不一致、源路径不明或工具行为可能写回共享盘时，立即停止并报告，不尝试“继续试试看”。
- 本地整理出错时，只删除或重建本机发布包中的副本；不得通过删除源文件来“清理重复”。
- 任务回滚点是本机发布包目录和其清单，不能把共享盘原图纳入回滚目标。发布前的预览、复制和检查均不改变远端状态。

## 默认文案规格（女性生活方式种草 v1.0）

除非用户指定其他人设，单篇日常种草默认采用“女生生活方式分享”视角。以下是可执行的 MUST/SHOULD 规则：

- **结构 MUST**：开头 2-3 行先写一个具体生活痛点（如脱妆、等车、排队、背小包），再自然引出产品；中段用 3-4 个场景化优点；参数集中放在末尾“参数小抄”，避免正文变成说明书。
- **语气 MUST**：像和姐妹聊天，使用“姐妹/宝子/真的/刚好/不占地方”等自然口语；每 2-3 句出现一次 emoji，但不连续堆叠感叹号。
- **人味 MUST**：允许第一人称表达审美或选择偏好（如“我会更在意轻不轻”），但不得虚构未发生的亲测、使用时长、回购、效果或订单。
- **参数 SHOULD**：正文只保留 1-3 个与场景直接相关的参数，其余放入 2-4 行参数小抄；所有数值必须与用户资料一致，并保留“标注/约/实际因条件变化”等边界。
- **标题 MUST**：给出 5 个备选；每个不超过 20 个字符，包含 1 个 emoji 和第二人称或明确人群词；避免“第一、最好、闭眼冲”等无法证明的绝对化表达。
- **标签 MUST**：给出 8-15 个，按热门、品类、人群、场景、长尾混合；不得把未确认功效或比较结论写进标签。
- **视觉 SHOULD**：优先 3:4 竖版、主体清晰、生活场景真实、整组比例统一；封面只突出一个核心画面或一句短钩子。

### 文案验收门

成稿必须同时满足：读起来像真人聊天；首段有具体场景；正文参数不过载；没有虚构体验或夸大功效；标题/段落/emoji/标签符合上面的数量与长度要求；每个卖点都能追溯到内部简报或素材可见信息。

## 调用语义

用户提出真实业务需求时，使用用户原始业务目标作为任务提示，不把它改写成“测试”、冒烟或演示。先识别意图并按需收集字段：登录或检查登录不要求完整模板；已有任务发布或定时发布先检索本地 `任务索引.json`，默认沿用已保存文案、素材和产品资料；只有创建内容、补充任务或资料缺失时才询问对应的配图、文案和产品信息模式。例如用户说“我现在需要用小红书写一篇关于 PE01 的图文，帮我写一篇”，才按配置选择自动或混合配图并按 `平面组 -> 品牌组 -> 型号` 查找中文店铺图片，再按配置处理用户资料、文案和素材；最后根据任务动作进入发布、保存或定时预览。若用户明确说直接发布，则完成质检后直接发布；这不自动替代定时任务确认。登录前不访问共享盘、不复制素材、不写文案。

## 状态

每次汇报使用一个状态：`待登录`、`待任务配置`、`待补资料`、`待复制素材`、`待素材检查`、`待保存`、`已创建待补内容`、`已保存`、`待确认发布`、`待定时发布`、`定时发布中`、`待重新登录`、`已发布` 或 `受阻`。说明状态变化、已完成的交付物和下一步最小动作；仅登录或检查登录的任务在验收后直接汇报登录结果，不强行进入 `待任务配置`；“已上传”不等于“已发布”，“已创建待补内容”不等于“已保存”，“已保存”不等于“已定时”。用户已明确直接发布时，不进入`待确认发布`停留。
