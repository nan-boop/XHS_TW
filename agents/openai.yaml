interface:
  display_name: "小红书产品图文发布"
  short_description: "按需登录、按文案类型写稿并安全发布，支持保存及定时发布"
  default_prompt: "使用 $xiaohongshu-publishing。登录成功后用中文说明选项，并把【小红书任务配置】单独放进 text 纯文本代码块高亮发给用户。有定时意向时先按发布时间搜文件夹：用户指定了路径就去指定位置，没指定就去 F:\\注册机\\小红书发布。发布已有任务才跳过这张卡。"
policy:
  allow_implicit_invocation: true
